# 09 — Checked and found correct (do not re-flag)

Properties the reviewers explicitly verified on v0.5.0. Re-verify only if the
code they rest on changes.

| ID | Property | Evidence |
|---|---|---|
| VER-01 | **FFI signatures match:** all 172 `extern "C"` decls in `src/ffi.rs` match `csrc/ferrompi.h`/`.c` in arity, widths (`i32`/`i64`/ptr) and return types; `int32_t* → int*` casts in v-collectives sound (`int` is 32-bit on targets) | automated comparison `repros/tools/ffi_cmp.py` → `checked 172 bad 0` |
| VER-02 | **Sealed traits:** `MpiDatatype`, `MpiIndexedDatatype`, `BytePermutable`, `AtomicMpiDatatype` sealed via private supertraits (`src/datatype.rs:42-65`) with `compile_fail` doctests; `BytePermutable` limited to padding-free integers (+ arrays) — sound for `MPI_BYTE`; `AtomicMpiDatatype` excludes `f32`/`f64` per MPI 4.1 §12.5.4; `allreduce_bytes` byte-count math overflow-checked (`src/comm/blocking.rs:490`) | reading + doctests |
| VER-03 | **Single-request bookkeeping:** `Request::wait` sets `completed = true` before the FFI call (`src/request.rs:139`) and `Drop` is guarded (`:429`); `PersistentRequest` wait-then-`request_free` ordering (`src/persistent.rs:232-248`); C `ferrompi_wait` frees the slot only when `*req == MPI_REQUEST_NULL` (persistent requests not auto-freed) | reading |
| VER-04 | **Late drops after finalize** of `Communicator`, `Group`, `Info`, `CustomDatatype`, `Request`, `PersistentRequest`, `Win` are safe no-ops (finalize clears the tables; `comm_free` returns success). Gaps: `UserOp` (COR-07) and window *memory* (SND-10) | repro (t15, t11 control paths) + reading |
| VER-05 | **`recv`/`recv_custom` count is `buf.len()`** (`src/comm/p2p.rs:73`) → an oversized incoming message is an MPI truncation error, not an overflow | reading + errclass repro |
| VER-06 | **Pair types on LP64 Linux:** MPICH extents `FLOAT_INT` 8, `DOUBLE_INT` 16, `LONG_INT` 16, `2INT` 8, `SHORT_INT` 8, `LONG_DOUBLE_INT` 32 equal the Rust `#[repr(C)]` sizes (x86_64, aarch64, ppc64le layouts verified). macOS arm64 / Windows are wrong (COR-11) | `repros/c-shim/c/pair_type_extents.c` |
| VER-07 | **Request bitmap allocation** cannot double-claim (idempotent `fetch_or` retry); release/acquire pairing between free and lookup correct; every `get_*` bounds-checks incl. negative handles; `COMM_WORLD` (slot 0) and `GROUP_EMPTY` (slot 0) can be neither allocated nor freed; all seven tables use atomic occupancy | reading |
| VER-08 | **`MPI_IN_PLACE` usage:** reduce (root `IN_PLACE`, non-root `buf,buf`), allreduce/allgather/alltoall in-place, gather in-place root-only by design, scatter in-place (root `recvbuf = IN_PLACE`, non-root NULL sendbuf) | reading + `repros/c-shim/src/bin/t12_reduce_inplace.rs` OK on MPICH |
| VER-09 | **`UserOp` safety:** closure call wrapped in `catch_unwind` + `process::abort` (`src/op.rs:208-216`) — no unwinding across `extern "C"`; closure `Send + Sync + 'static`; boxed closure dropped only after `MPI_Op_free` returns (`csrc/ferrompi.c:4923-4946`); rollback on `MPI_Op_create` failure never frees `MPI_OP_NULL`. **Caveat:** safe only because user ops are used by blocking allreduce alone — `MPI_Op_free` merely marks for deallocation, so nonblocking/persistent user-op reductions (ARC-07) require the op to outlive pending operations | reading |
| VER-10 | **Table-full teardown:** active requests that cannot be registered are completed with `MPI_Wait` (`complete_unregistered_request`, `csrc/ferrompi.c:351-379`), inactive `*_init` requests freed with `MPI_Request_free` — the correct teardown for each | reading |
| VER-11 | **`MPI_ERRORS_RETURN` installed** on every created communicator (dup, split, split_type, create, create_from_group) and, best effort, windows | reading |
| VER-12 | **Large-count dispatch** for blocking/nonblocking non-v ops on MPI 4 uses `_c` above `INT_MAX`; `MPI_Get_count_c` used; persistent ops reject with `MPI_ERR_COUNT` (COR-05 covers the gaps) | reading |
| VER-13 | **Datatype mapping:** fixed-width MPI types (`u64 → MPI_UINT64_T` etc.) | reading |
| VER-14 | **String buffers:** error string 512 ≥ MPICH 512 / Open MPI 256; processor name 256; library version 8192; `snprintf` clamped; `from_utf8`/`CStr` bounded by returned length | reading |
| VER-15 | **`MPI_Group_free(MPI_GROUP_EMPTY)`** accepted by MPICH | `repros/c-shim/c/gfree.c`, `t10_group_empty.rs` |
| VER-16 | **`MPI_Error_class`/`MPI_Error_string` after finalize** work on MPICH | `repros/c-shim/src/bin/t15_err_after_finalize.rs` |
| VER-17 | **C build hygiene:** strict `-Wall -Wextra -Wconversion` clean except one false-positive at `type_create_struct` (COR-16) | compile |
| VER-18 | **Design decisions judged sound:** `PendingFetchResult` owned boxes + `unsafe fn resolve` (pattern to generalise, SND-04 aside); `Win<'a, T>` `PhantomData<&'a mut [T]>` + RAII lock guards with `flush` only on guards; `ResourceExhausted{ResourceKind}` typed mapping; cached rank/size in `Communicator`; slice-based `wait_all`/`start_all` with ≤ 64 stack scratch; `PersistentRequest` runtime active-state check (vs typestate) given slice APIs; `MPI_UNDEFINED` normalised to −1 in group translate/waitany; `MPI_MODE_*` queried at runtime; `GroupComparison` normalised in C; `abort() -> !` falls back to `process::abort`; `create_from_group` probe does not cache failures; `ThreadLevel: Ord` | reading |
| VER-19 | **Blocking-path performance** is optimal (~1 ns overhead); cross-language LTO correctly not pursued — full list in [05-performance](05-performance.md#verified-efficient--do-not-optimise) | measured |
| VER-20 | **Docs that are accurate:** supported-type lists (f32, f64, i32, i64, u8, u32, u64) across README/lib.rs/datatype.rs/architecture.md; 15 nonblocking `i*` collectives; p2p and communicator-management lists; Group method names; the Send/Sync table's field shapes (`src/lib.rs:122-136`); `-1 → MPI_ANY_SOURCE/ANY_TAG` in every recv/probe shim; README handle-table sizes (256/16384/256/64); SLURM function↔env-var table; `Request::cancel` Open MPI note; CHANGELOG compare links; `publish.yml` tag/version check | reading |
